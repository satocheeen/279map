{
    "env": {
        "NODE_ENV": "dev"
    },
    "watch": ["src"],
    "ext": "ts, js, json",
    "exec": "node --inspect=0.0.0.0:9228 --enable-source-maps -r ts-node/register -r dotenv/config ./src/index.ts dotenv_config_path=.env.dev"  
}
