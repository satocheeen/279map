type Hoge {
    message: String!
}
type Target {
    id: DataId!
    wkt: String!
}
type Subscription {
    test: Hoge!
    test2(type: String!): Hoge!
    itemInsert(mapId: String!, mapKind: MapKind!): [Target!]!
}
